import java.awt.*;

public class Main {

    public static void main(String[] args){
        PrincipalFrame principalFrame = new PrincipalFrame();
        principalFrame.setSize(new Dimension(principalFrame.getWidth()-1,principalFrame.getHeight()-1));
    }

}
